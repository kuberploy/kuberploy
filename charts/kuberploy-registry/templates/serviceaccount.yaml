{{- if .Values.enabled }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kuberploy-registry.serviceAccountName" . }}
  labels:
    {{- include "kuberploy-registry.labels" . | nindent 4 }}
automountServiceAccountToken: false
{{- end }}
