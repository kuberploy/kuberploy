{{- $active := .Values.bootstrap.argoCD.enabled -}}
{{- range $_, $component := .Values.components -}}
  {{- if $component.enabled -}}{{- $active = true -}}{{- end -}}
{{- end -}}
{{- if $active }}
The installer submitted independently owned Argo CD Applications.

Helm success proves only that the bootstrap resources were accepted. It does
not prove that child Applications are Synced/Healthy or that Kuberploy runtime
readiness probes and capability attestations are ready. Inspect every enabled
Application and the Kuberploy readiness API before admitting traffic.
{{- if .Values.components.controlPlane.enabled }}

When the control-plane value file enables in-cluster bootstrap-token generation,
retrieve its one-time disclosure after that child Application syncs:

  kubectl -n kuberploy-system logs job/kuberploy-bootstrap-token

The token is generated by the child chart hook and is never stored in this
installer release. An existing bootstrap Secret is never re-disclosed.
{{- end }}
{{- else }}
Kuberploy installer is disabled. No bootstrap or child resources were rendered.
{{- end }}
