{
  "breakingChanges": true,
  "summary": "Connected Git SSH Apps expose Deploy, Rebuild, and build history using the existing source build workflow. ExternalDNS readiness follows the current Deployment rollout rather than an older ready Pod. App headers distinguish an intentionally stopped deployment from an unknown runtime state.",
  "supportedUpgradeFrom": ">=0.1.0 <0.2.0",
  "version": "0.1.0-rc.462"
}
