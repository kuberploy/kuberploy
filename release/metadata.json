{
  "breakingChanges": true,
  "summary": "Schema 017 is current. RC188 keeps email as the only local login identity, makes display names presentation-only, revokes the prior browser session when an invitation is accepted, and includes an audited offline recovery path for legacy administrators; it also carries fail-closed admission, public provider egress, tunable infrastructure, read-only Argo observation, GitHub retry handling, and historical source-build reads.",
  "supportedUpgradeFrom": ">=0.1.0 <0.2.0",
  "version": "0.1.0-rc.188"
}
