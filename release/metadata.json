{
  "breakingChanges": true,
  "summary": "Schema 018. RC263 keeps email-only login, separates auth limits, cleans installer hooks and registry-pull artifacts, reconstructs External DNS profiles, follows system theme, purges accepted invites, observes runtime/Argo readiness, retries providers/GHCR, validates GitHub delivery IDs, removes failed build auxiliaries, qualifies independent retries, splits safe build-log scope stages including Job namespace/name binding, publishes automatically, and rejects stale installer versions before Argo drift.",
  "supportedUpgradeFrom": ">=0.1.0 <0.2.0",
  "version": "0.1.0-rc.263"
}
