{
  "breakingChanges": true,
  "summary": "Administrators can deactivate a degraded certificate issuer even when that issuer makes platform readiness unhealthy, restoring the exact cleanup path without weakening protected Git checks.",
  "supportedUpgradeFrom": ">=0.1.0 <0.2.0",
  "version": "0.1.0-rc.395"
}
