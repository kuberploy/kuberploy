{
  "breakingChanges": true,
  "summary": "Schema 018. RC263 keeps email-only login, separates auth limits, cleans installer hooks, reconstructs External DNS profiles, follows system theme, purges accepted invites, observes runtime/Argo readiness, retries providers/GHCR, validates GitHub delivery IDs, removes failed build auxiliaries, qualifies independent retries, splits build-log scope stages including Job binding, publishes automatically, and rejects stale installer versions.",
  "supportedUpgradeFrom": ">=0.1.0 <0.2.0",
  "version": "0.1.0-rc.264"
}
